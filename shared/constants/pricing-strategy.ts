import type { PricingStrategy } from '@/shared/types/enums'

export const PRICING_STRATEGY_LABELS: Record<PricingStrategy, string> = {
  POR_ESTRUCTURA: 'Por estructura',
  NO_VINCULAR: 'No vincular',
}
